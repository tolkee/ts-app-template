interface:
  display_name: "Initialize Deployment"
  short_description: "Release this fork through Railway and Vercel"
  default_prompt: "Use $init-deploy to deploy this fork with Railway, Vercel, and GitHub previews."
